<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="182" onload="init(evt)" viewBox="0 0 1200 182" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = true;
var searchcolor = 'rgb(230,0,230)';]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    searching = 0;
}

window.addEventListener("click", function(e) {
	var target = find_group(e.target);
	if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);
    }
    else if (e.target.id == "unzoom") unzoom();
    else if (e.target.id == "search") search_prompt();
}, false)

// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
	var target = find_group(e.target);
	if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
	var target = find_group(e.target);
	if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function find_child(node, selector) {
	var children = node.querySelectorAll(selector);
	if (children.length) return children[0];
	return;
}
function find_group(node) {
	var parent = node.parentElement;
	if (!parent) return;
	if (parent.id == "frames") return node;
	return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) -3;
    var txt = find_child(e, "title").textContent.replace(/\\([^(]*\\)\$/,"");
    t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
        return;
    for (var x = txt.length - 2; x > 0; x--) {
        if (t.getSubStringLength(0, x + 2) <= w) {
            t.textContent = txt.substring(0, x) + "..";
            return;
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - xpad) * ratio + xpad;
            if(e.tagName == "text")
                e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x - xpad, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = xpad;
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = parseInt(svg.width.baseVal.value) - (xpad*2);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = parseFloat(xmin + width);
    var ymin = parseFloat(attr.y.value);
    var ratio = (svg.width.baseVal.value - 2 * xpad) / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.0001;
    unzoombtn.classList.remove("hide");
    var el = document.getElementById("frames").children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = document.getElementById("frames").children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = document.getElementById("frames").children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;	// JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
]]></script><rect x="0" y="0" width="1200" height="182" fill="url(#background)"/><text id="title" x="600.00" y="24.00">py-spy</text><text id="details" x="10.00" y="165.00"> </text><text id="unzoom" class="hide" x="10.00" y="24.00">Reset Zoom</text><text id="search" x="1090.00" y="24.00">Search</text><text id="matched" x="1090.00" y="165.00"> </text><g id="frames"><g><title>fit (implicit/als.py:130) (48 samples, 1.66%)</title><rect x="10" y="84" width="19" height="15" fill="rgb(209,165,42)"/><text x="13.00" y="94.50"></text></g><g><title>tocsr (scipy/sparse/csc.py:151) (48 samples, 1.66%)</title><rect x="10" y="100" width="19" height="15" fill="rgb(224,50,14)"/><text x="13.00" y="110.50"></text></g><g><title>fit (implicit/als.py:138) (8 samples, 0.28%)</title><rect x="29" y="84" width="3" height="15" fill="rgb(226,58,24)"/><text x="32.00" y="94.50"></text></g><g><title>fit (implicit/als.py:140) (5 samples, 0.17%)</title><rect x="32" y="84" width="2" height="15" fill="rgb(246,30,43)"/><text x="35.00" y="94.50"></text></g><g><title>fit (implicit/als.py:159) (625 samples, 21.60%)</title><rect x="34" y="84" width="255" height="15" fill="rgb(233,24,16)"/><text x="37.00" y="94.50">fit (implicit/als.py:159)</text></g><g><title>calculate_similar_artists (lastfm.py:79) (1,490 samples, 51.50%)</title><rect x="10" y="68" width="607" height="15" fill="rgb(209,216,44)"/><text x="13.00" y="78.50">calculate_similar_artists (lastfm.py:79)</text></g><g><title>fit (implicit/als.py:163) (804 samples, 27.79%)</title><rect x="289" y="84" width="328" height="15" fill="rgb(252,227,46)"/><text x="292.00" y="94.50">fit (implicit/als.py:163)</text></g><g><title>calculate_similar_artists (lastfm.py:87) (20 samples, 0.69%)</title><rect x="617" y="68" width="8" height="15" fill="rgb(219,164,21)"/><text x="620.00" y="78.50"></text></g><g><title>&lt;lambda&gt; (lastfm.py:87) (7 samples, 0.24%)</title><rect x="623" y="84" width="2" height="15" fill="rgb(230,95,21)"/><text x="626.00" y="94.50"></text></g><g><title>similar_items (implicit/recommender_base.py:201) (5 samples, 0.17%)</title><rect x="625" y="84" width="2" height="15" fill="rgb(237,61,12)"/><text x="628.00" y="94.50"></text></g><g><title>item_norms (implicit/recommender_base.py:223) (5 samples, 0.17%)</title><rect x="625" y="100" width="2" height="15" fill="rgb(207,39,41)"/><text x="628.00" y="110.50"></text></g><g><title>norm (numpy/linalg/linalg.py:2389) (5 samples, 0.17%)</title><rect x="625" y="116" width="2" height="15" fill="rgb(247,184,30)"/><text x="628.00" y="126.50"></text></g><g><title>_get_similarity_score (implicit/recommender_base.py:208) (545 samples, 18.84%)</title><rect x="628" y="100" width="222" height="15" fill="rgb(232,74,45)"/><text x="631.00" y="110.50">_get_similarity_score (implic..</text></g><g><title>_get_similarity_score (implicit/recommender_base.py:209) (814 samples, 28.14%)</title><rect x="850" y="100" width="332" height="15" fill="rgb(248,158,16)"/><text x="853.00" y="110.50">_get_similarity_score (implicit/recommender_..</text></g><g><title>argpartition (numpy/core/fromnumeric.py:757) (813 samples, 28.10%)</title><rect x="851" y="116" width="331" height="15" fill="rgb(248,223,24)"/><text x="854.00" y="126.50">argpartition (numpy/core/fromnumeric.py:757)</text></g><g><title>_wrapfunc (numpy/core/fromnumeric.py:51) (813 samples, 28.10%)</title><rect x="851" y="132" width="331" height="15" fill="rgb(214,179,45)"/><text x="854.00" y="142.50">_wrapfunc (numpy/core/fromnumeric.py:51)</text></g><g><title>calculate_similar_artists (lastfm.py:95) (1,371 samples, 47.39%)</title><rect x="625" y="68" width="560" height="15" fill="rgb(228,126,42)"/><text x="628.00" y="78.50">calculate_similar_artists (lastfm.py:95)</text></g><g><title>similar_items (implicit/recommender_base.py:203) (1,366 samples, 47.22%)</title><rect x="627" y="84" width="558" height="15" fill="rgb(225,127,24)"/><text x="630.00" y="94.50">similar_items (implicit/recommender_base.py:203)</text></g><g><title>_get_similarity_score (implicit/recommender_base.py:210) (6 samples, 0.21%)</title><rect x="1182" y="100" width="3" height="15" fill="rgb(232,148,5)"/><text x="1185.00" y="110.50"></text></g><g><title>calculate_similar_artists (lastfm.py:96) (11 samples, 0.38%)</title><rect x="1185" y="68" width="4" height="15" fill="rgb(215,34,32)"/><text x="1188.00" y="78.50"></text></g><g><title>write (codecs.py:721) (4 samples, 0.14%)</title><rect x="1187" y="84" width="2" height="15" fill="rgb(230,15,17)"/><text x="1190.00" y="94.50"></text></g><g><title>all (2,893 samples, 100%)</title><rect x="10" y="36" width="1180" height="15" fill="rgb(218,49,21)"/><text x="13.00" y="46.50"></text></g><g><title>&lt;module&gt; (lastfm.py:161) (2,893 samples, 100.00%)</title><rect x="10" y="52" width="1180" height="15" fill="rgb(213,40,38)"/><text x="13.00" y="62.50">&lt;module&gt; (lastfm.py:161)</text></g></g></svg>